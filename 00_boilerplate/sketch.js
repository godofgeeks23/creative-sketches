// global variables
var canvasWidth = 700;
var canvasHeight = 700;

var backgroundGrey = 51;

function setup() {
  createCanvas(canvasWidth, canvasHeight);
}

function draw() {
  background(backgroundGrey);
  stroke(255);
}
